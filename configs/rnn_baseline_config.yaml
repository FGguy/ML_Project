model: rnn
downsample_factor: 256
hidden_size: 64
num_classes: 6
batch_size: 16
lr: 0.001
weight_decay: 0.0001
scheduler: cosine
epochs: 100
patience: 15
num_workers: 0
# Baseline keeps mixup and class-weight improvements for a fair comparison.
mixup_alpha: 0.0
use_class_weights: true
